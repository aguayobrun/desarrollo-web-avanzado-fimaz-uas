tenemos como base ironicamente la base de datos en la carpeta config que nos sirve como conexion a la base de datos, el incluir $connection sirve para tambien evitar injecciones de mysql y asi que no roben la info de nuestra base de datos, despues el product controller que como traduccion tiene controllador del producto, en este caso es usado para hacer querys y que asi nuestra base de datos pueda añadir, eliminar actualizar y buscar, sirve para que le mande info a nuestro modelo que el modelo lo procese y lo lleve a la base de datos para que despues en el index le pueda mostrar al usuario los cambios realizados, claro esta sin usar 2 o mas pestañas para procesar todo esto, el autoload sirve para que a la hora de llamar ya sea el producto controlador en nuestro index y modelo no tener que procesarlo varias veces y asi acelerar la eficiencia en nuestro codigo ya que el tiempo es dinero, sutituyendo el require_once por namespace app\(nombre de carpete), y ya finalmente el index tambien puede ser usado como las views ya le muestra al usuario el diseño usado que esta mas enfocado en la practicidad que en el diseño como tal.